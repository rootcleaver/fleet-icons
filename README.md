# fleet-icons

Small square avatars used as `icon_url` for Slack bot messages.

- 256×256 PNG. Sources in `src/*.svg`.
- Filenames are intentionally neutral. Meaning lives with whoever uses them.
- Public so that Slack's servers can fetch them. Nothing else here.
